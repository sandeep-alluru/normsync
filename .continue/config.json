{
  "name": "normsync",
  "version": "0.1.0",
  "schema": "v1",
  "context": [
    {"provider": "file", "params": {"nRetrieve": 20, "nFinal": 5}},
    {"provider": "code"}
  ],
  "docs": [
    {
      "title": "normsync Architecture",
      "startUrl": "file://./ARCHITECTURE.md",
      "faviconUrl": ""
    }
  ],
  "customCommands": [
    {
      "name": "explain",
      "prompt": "Explain how this part of normsync works: {input}. Use the module map in AGENTS.md as context.",
      "description": "Explain a part of the codebase"
    }
  ],
  "rules": [
    "This is normsync — World constitution engine for norm-governed multi-agent games.",
    "Library code must not use print() — use rich.console.Console.",
    "All public API must have type annotations and docstrings.",
    "Tests use pytest. CLI tests use click.testing.CliRunner."
  ]
}
